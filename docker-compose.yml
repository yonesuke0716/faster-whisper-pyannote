services:
  transcribe_ai:
    container_name: transcribe_ai
    build:
      context: .
      dockerfile: ./Dockerfile
    image: transcribe_ai
    volumes:
      - ./src:/app
    tty: true
    environment:
      - HUGGING_FACE_TOKEN=$HUGGING_FACE_TOKEN
    # ------- GPU ------
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - capabilities: [gpu]
    # shm_size: "16gb"
    # -------------------
